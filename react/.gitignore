# Ignore everything by default
*

# Allow these core files and folders
!.gitignore
!home-portal.php
!composer.json
!composer.lock

# Include the src folder (but ignore its vendor)
!src/
src/vendor/

# Include the react folder (but ignore node_modules)
!react/
react/node_modules/

# Optional: Ignore React build and system files
react/dist/
react/build/
react/.env
react/.DS_Store
