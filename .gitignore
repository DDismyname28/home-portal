# Ignore everything by default
*

# Allow these core files and folders
!.gitignore
!home-portal.php
!composer.json
!composer.lock

# Include the src folder and everything inside (except vendor)
!src/
src/vendor/

# Include the react folder and everything inside (except node_modules)
!react/
react/node_modules/

# Optional: Ignore React build and system files
react/dist/
react/build/
react/.env
react/.DS_Store
